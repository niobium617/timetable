# 任务：开发微信小程序课表应用，UniApp(Vue3) + uView Plus
## 项目背景
个人小范围使用（最多几十人），数据优先存储在小程序本地，离线可用。
我有一台小型Linux服务器，后端仅用来做教务系统爬虫导入，**后端不存储课表、不做账号登录**。
小程序端完成全部课表渲染、单双周、假期调休逻辑；后端只提供爬虫接口，用于一键导入教务课表，爬虫失效时保留前端粘贴源码解析兜底导入。

## 整体要求
1. 使用 UniApp，Vue3 语法，uView‑Plus UI组件库，编译目标为微信小程序。
2. 全部业务计算（周次、单双周、调休、假期过滤）**全部在小程序前端完成，不依赖后端接口**。
3. 数据持久化使用 uni.setStorageSync 本地存储，使用JSON结构保存所有数据，不要使用本地数据库。
4. 代码结构清晰，注释完善，组件拆分合理，便于后续修改。
5. 不做用户登录账号体系，所有数据保存在用户设备本地。
6. 输出完整可运行项目，包含页面、组件、工具函数、存储管理器。
7. 同时输出后端FastAPI+Playwright爬虫框架说明（不需要写完整爬虫解析，只写框架模板，后续我自己适配学校教务）。

## 功能需求清单
### 1. 底部Tab导航
三个tab：【课表】、【日历】、【设置】

### 2. 周课表页面（课表tab）
- 网格视图：横向周一~周日，纵向是节次。
- 左右滑动切换上一周、下一周。
- 顶部显示：当前教学周号、【单周/双周】标识。
- 课程块展示课程名称、教室；点击课程块弹出编辑弹窗。
- 点击空白格子快速新增课程。
- 今日列高亮；当前节次显示参考线。
- 课程属性：课程名、教师、教室、颜色、备注；星期、开始节次、结束节次。
- 周类型：每周上课 / 仅单周 / 仅双周 / 自定义起止周。
- 支持课程编辑、删除、复制。

### 3. 月日历页面（日历tab）
- 月历视图，有课程的日期底部显示彩色圆点标记。
- 点击日期弹出底部弹窗，展示当天全部课程时间线。
- 假期日期置灰；调休日期显示「补」角标。
- 支持切换月份，悬浮按钮回到今天。
- 顶部显示当前教学周次、单双周状态。

### 4. 学期全局配置（设置页）
- 学期起始日期（第一周周一）
- 第一周是单周还是双周（可配置）
- 每周起始日：周一/周日
- 手动校准当前教学周（手动设置本周是第几周，覆盖自动计算）
- 自定义节次时间表：每一节的开始、结束时间。

### 5. 假期 & 调休管理（设置页）
#### 假期
- 添加单个日期 / 批量日期范围假期，假期当天**完全不显示课程**。
- 支持删除假期。

#### 调休（补班）
- 配置规则：`调休日期 → 补星期X的课`
- 渲染逻辑：调休当天不使用原本星期的课，而是加载目标星期的课程，同时依然执行单双周、起止周过滤。
> 渲染优先级规则（必须严格实现）
> 1. 如果该日期属于假期：不显示任何课程
> 2. 如果该日期属于调休：使用目标星期的课程列表，再过滤单双周、起止周
> 3. 否则：使用当天原生星期课程，过滤单双周、起止周

### 6. 课表导入功能（设置页）
#### 模式A：后端接口导入（调用我的小型服务器）
- 表单：选择学校，输入学号、密码，验证码输入框。
- 调用后端POST接口，获取结构化课程JSON。
- 导入前预览课程列表，支持【覆盖导入】/【增量导入】。
> 后端接口地址由配置文件管理，方便修改。

#### 模式B：前端兜底导入（不需要服务器）
1. HTML源码粘贴解析：用户复制教务网页源码粘贴，前端正则解析课程，预览导入。
2. 文本粘贴解析。
> 解析器只做模板，不需要适配特定学校，留好扩展接口。

### 7. 数据备份与恢复
- 导出全部课表数据为JSON文本，可以复制保存。
- 导入JSON备份恢复全部数据。

### 8. 工具函数（必须独立封装）
1. 教学周次计算函数：输入日期，返回当前教学周号、单/双周标记。
2. 根据日期，过滤得到该日期的全部课程列表（完整执行假期、调休、单双周过滤逻辑）。
3. 课程数据的增删改查管理器，统一封装，不要散落在各个页面。

## 本地存储JSON数据结构（严格按照这个结构）
```json
{
  "config": {
    "termStartDate": "2026-09-01",
    "firstWeekType": "odd", // odd单周，even双周
    "weekStartDay": 1, // 1=周一，7=周日
    "manualWeek": null, // null代表自动计算；数字代表手动强制周次
    "sections": [
      {"section":1,"startTime":"08:00","endTime":"08:45"},
      {"section":2,"startTime":"08:55","endTime":"09:40"}
    ]
  },
  "courses": [
    {
      "id":1,
      "name":"高等数学",
      "teacher":"张老师",
      "classroom":"101",
      "color":"#409eff",
      "remark":"",
      "weekday":1, //1-7，周一到周日
      "startSection":1,
      "endSection":2,
      "weekType":"weekly", // weekly每周，odd仅单周，even仅双周，custom自定义
      "startWeek":1,
      "endWeek":16
    }
  ],
  "holidays": [
    {"date":"2026-10-01","name":"国庆节"}
  ],
  "adjustments": [
    {"date":"2026-10-07","targetWeekday":3,"remark":"补周三的课"}
  ]
}