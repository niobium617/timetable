# 微信小程序课表应用 · 方案优化版 v2

> 基于 `方案1` 审查后优化。文首为审查结论与改动对照表；正文为完整可执行规格。
> 标注说明：【改】对原方案字段/逻辑的修改，【加】新增内容，【明确】对未定义语义的补充，其余沿用原方案。

---

## 〇、审查结论与主要改动

| # | 原方案问题 | 优化措施 |
|---|-----------|---------|
| 1 | 周规则仅支持单段起止周（startWeek/endWeek），无法表达"2-8周、10-14周"不连续周段 | 【改】合并为统一 `weeks` 字段：`"all" / "odd" / "even" / "1-16" / "2-8,10-14"` |
| 2 | 调休当天过滤单双周/起止周时"用哪个教学周"未定义 | 【明确】一律用**调休当天**的教学周号判定 |
| 3 | 教学周号与假期关系未定义 | 【明确】周号=纯日期差每 7 天 +1；假期**不跳号**（周次照走，只是当天没课） |
| 4 | `manualWeek` 与单双周的关系未定义 | 【明确】设置 manualWeek 后，单双周随其计算，不再用自动周号 |
| 5 | 同节次多课（冲突/并排）的渲染未定义 | 【加】同节次多课横向均分并排算法 + 导入冲突检测 |
| 6 | 数据模型缺 version、id 生成规则、增量导入匹配键 | 【加】`version` 字段、`id` 生成规则、`sourceKey` 去重键 |
| 7 | 后端接口契约、爬虫适配器结构、安全要求未定义 | 【加】完整 API 契约 + SchoolAdapter 接口 + 安全清单 |
| 8 | 无实施路线图、无核心函数测试 | 【加】四阶段路线图 + 纯函数测试用例表 |
| 9 | 示例 termStartDate 用了 2026-09-01（实际是周二） | 【明确】termStartDate 必须为周一，设置页校验并提示；示例改为 2026-08-31 |
| 10 | 未考虑微信小程序存储上限与体验版成员上限 | 【明确】单 key 1MB/总 10MB，数据量足够但需写入兜底；几十人使用注意体验版成员上限 |

**原方案保留不变的部分**：整体架构（前端完成全部业务逻辑、后端只做爬虫）、本地 JSON 存储、三大 Tab、功能清单主体、渲染优先级总原则（假期 > 调休 > 正常）。

---

## 一、项目背景与架构（不变）

- 个人小范围使用（最多几十人），数据全部存在用户设备本地，离线可用。
- 自有 Linux 服务器仅承担"教务系统爬虫导入"，**后端不存储课表、不做账号登录**。
- 爬虫失效时保留前端粘贴 HTML/文本源码解析的兜底导入。

### 架构边界（明确）
- 小程序端：全部课表渲染、周次、单双周、假期、调休逻辑，以及数据持久化。
- 后端：只做一件事——接收（学校、学号、密码、验证码）→ 抓取教务课表 → 返回结构化 JSON。无状态、无数据库、请求结束即释放。
- 两者间唯一的契约是"课程 JSON 结构"（见第四节），后端负责把学校原始课表转换为该结构。

---

## 二、整体要求（沿用 + 补充）

1. UniApp（Vue3 语法）+ uView Plus，编译目标微信小程序。（保留）
2. 全部业务计算在前端完成，不依赖后端接口。（保留）
3. 数据持久化用 `uni.setStorageSync` 存 JSON 结构，不引入本地数据库。（保留）
   - 【加】微信小程序存储：单 key 上限 1MB、总上限 10MB。本应用数据量为 KB 级，无压力；但写入统一走存储管理器，内部 try/catch，失败时弹窗提示"存储异常，请先导出备份"。
4. 代码结构清晰、注释完善、组件拆分合理。（保留）
5. 无登录账号体系，数据在用户设备本地。（保留）
6. 输出完整可运行项目，含页面、组件、工具函数、存储管理器。（保留）
7. 输出后端 FastAPI + Playwright 爬虫框架（接口契约 + 适配器模板，具体解析后续自适配）。（保留）

---

## 三、功能需求清单

### 1. 底部 Tab 导航（不变）
【课表】【日历】【设置】三 Tab。

### 2. 周课表页面（课表 Tab）
- 网格视图：横向周一~周日，纵向节次。（不变）
- 左右滑动切换周次：用 swiper 预渲染 3 页（上/当前/下），保证滑动流畅。（原方案无实现细节，明确采用此方案）
- 顶部：当前教学周号、【单/双周】标识。（不变）
- 课程块显示课程名、教室；点击弹出编辑弹窗；点空白格快速新增。（不变）
- 今日列高亮；当前节次参考线（当前时间落在哪个节次，由 sections 时间表换算）。（不变）
- 课程属性：课程名、教师、教室、颜色、备注、星期、开始/结束节次、周规则。（字段见第四节）
- 课程编辑、删除、复制。（不变）
- 【加】**同节次多课并排**：同一星期同一节次存在多门课时（冲突或手动双课），课程块横向均分并排渲染，互不遮挡；节次列高度按课程数量分配。
- 【加】回到"本周"按钮（快速跳回当前周）。

### 3. 月日历页面（日历 Tab）
- 月历视图，有课的日期显示彩色圆点。（不变）
- 点击日期弹出底部弹窗，展示当天课程时间线。（不变）
- 假期置灰、调休日期显示「补」角标。（不变）
- 切换月份、悬浮"回到今天"。（不变）
- 顶部显示当前教学周次、单双周。（不变）
- 【明确】月历组件建议自行封装或选支持自定义角标的三方组件（uView 的 u-calendar 角标能力不足）；每日课程即调用 `getCoursesOfDate`，成本极低无需缓存。
- 【明确】"今天"的判定与渲染规则与周课表同一套过滤链，保证两页一致。

### 4. 学期全局配置（设置页）
- 学期起始日期（第一周周一）。（不变）
  - 【明确】**必须为周一**，保存时校验并提示（原示例 2026-09-01 实为周二）。若实际开学日非周一，约定以开学日所在周的周一同日期为准录入。
- 第一周是单周还是双周（可配置）。（不变）
- 每周起始日：周一/周日。（不变）
  - 【明确】`weekStartDay` **只影响 UI 列顺序与月历周起始，不影响周号计算**。教学周号恒以 termStartDate（周一）起每 7 天一周；课程 `weekday` 恒为绝对星期（1=周一）。
- 手动校准当前教学周（覆盖自动计算）。（不变）
  - 【明确】设置后单双周随之计算（见第五节公式）。
- 自定义节次时间表：每节开始/结束时间。（不变）
- 【加】学校名、学期名（仅展示用）。

### 5. 假期 & 调休管理（设置页）
#### 假期
- 添加单个日期 / 日期范围批量；假期当天**完全不显示课程**。（不变）
- 支持删除。（不变）

#### 调休（补班）
- 配置规则：`调休日期 → 补星期X的课`。（不变，即 targetWeekday）
- 【明确】单双周/起止周过滤使用**调休当天**的教学周号（课在当天上，周次自然按当天算）。
- 【加】若未来学校口径是"补 X 月 X 日的课"，只需把 targetWeekday 换为 targetDate 并按那天的 weekday 解析——当前数据结构留好扩展余地，接口层统一返回 weekday 即可。

> **渲染优先级规则（必须严格实现）**
> 1. 该日期属于假期 → 不显示任何课程（即使同时是调休，假期优先）。
> 2. 该日期属于调休 → 取目标星期的课程列表，用**当天教学周号**过滤单双周、起止周。
> 3. 否则 → 用当天原生星期课程，用当天教学周号过滤。
> 【加】设置页录入时，若某日期同时命中假期与调休，保存时提示冲突。

### 6. 课表导入功能（设置页）
#### 模式 A：后端接口导入（自有服务器）
- 表单：选择学校、学号、密码、验证码输入框。（不变）
- 调用后端接口获取结构化课程 JSON。（接口契约见第六节）
- 导入前预览课程列表，支持【覆盖导入】/【增量导入】。（不变）
  - 【明确】**增量导入匹配键**：优先 `sourceKey`（后端返回的课程唯一标识），无则按 名称+星期+开始节次 匹配，命中则更新、未命中则新增。匹配结果在预览页分"新增/更新/不变"三组展示。
- 后端接口地址由配置文件管理。（不变）
- 【加】**冲突检测**：预览阶段对"同星期同节次重叠"的课程标红提示，由用户决定保留哪一门（不自动删）。

#### 模式 B：前端兜底导入（无需服务器）
1. HTML 源码粘贴解析（前端正则/遍历 DOM 解析，预览导入）。（不变）
2. 纯文本粘贴解析。（不变）
- 【明确】解析器定义统一接口 `Parser.parse(source) -> {courses, warnings}`，以"正方教务『课程表打印页』表格结构"为参考模板写好一份通用实现，留扩展位给其他学校；解析注意 colspan 合并单元格（大节）的处理。

### 7. 数据备份与恢复（不变）
- 导出全部数据为 JSON 文本（可复制保存）；导入 JSON 备份恢复。
- 【加】恢复前先自动备份当前数据，恢复可撤销。

---

## 四、本地存储 JSON 数据结构（改后严格按此结构）

```json
{
  "version": 1,
  "config": {
    "schoolName": "",
    "termName": "2026-2027学年第一学期",
    "termStartDate": "2026-08-31",
    "firstWeekType": "odd",
    "weekStartDay": 1,
    "manualWeek": null,
    "sections": [
      {"section": 1, "startTime": "08:00", "endTime": "08:45"},
      {"section": 2, "startTime": "08:55", "endTime": "09:40"}
    ]
  },
  "courses": [
    {
      "id": "c_1756xxxxxx01",
      "name": "高等数学",
      "teacher": "张老师",
      "classroom": "101",
      "color": "#409eff",
      "remark": "",
      "weekday": 1,
      "startSection": 1,
      "endSection": 2,
      "weeks": "all",
      "sourceKey": null
    }
  ],
  "holidays": [
    {"date": "2026-10-01", "name": "国庆节"}
  ],
  "adjustments": [
    {"date": "2026-10-07", "targetWeekday": 3, "remark": "补周三的课"}
  ]
}
```

**改动说明：**

1. 【改】**周规则合并为 `weeks` 字段**，废弃 `weekType / startWeek / endWeek` 三字段：
   - `"all"` 每周上；`"odd"` 仅单周；`"even"` 仅双周；
   - `"1-16"` 第 1~16 周；`"2-8,10-14"` **多段不连续周**。
   - 判定函数 `isCourseOnWeek(course, week)` 内部解析一次并缓存，性能无忧。
2. 【加】`id` 生成规则：`c_` + 时间戳 + 随机 2 位，保证唯一且稳定（编辑/删除/增量更新均依赖它）。
3. 【加】`sourceKey`：后端返回的原始课表标识，供增量导入去重/匹配（可为 null）。
4. 【加】顶层 `version`：后续结构变更时做迁移。
5. `holidays` / `adjustments` 结构不变；`adjustments.targetWeekday` 语义 = "补星期X的课"。

---

## 五、核心工具函数（独立封装，纯函数）

`utils/week.js`、`utils/filter.js`、`utils/weeksPattern.js`、`store/index.js`（存储管理器）

```js
// —— weeksPattern.js ——
parseWeeksPattern(pattern)  // "2-8,10-14" -> Set{2,3,...,8,10,...,14}；"all"/"odd"/"even" 特殊处理
isCourseOnWeek(course, weekNum)  // 依据 course.weeks 判定

// —— week.js ——
// 输入日期，返回教学周信息；termStartDate 必须为周一
getWeekInfo(dateStr):
  dayDiff = (date - termStartDate) / 86400000
  weekNum = floor(dayDiff / 7) + 1
  // firstWeekType = 'odd'  → 单周判定：weekNum 为奇数
  // firstWeekType = 'even' → 单周判定：weekNum 为偶数
  isOddWeek = (weekNum % 2 === 1) === (firstWeekType === 'odd')
  // manualWeek 非 null 时：weekNum = manualWeek，且 isOddWeek 用 manualWeek 计算

// —— filter.js —— 完整过滤链（唯一入口，周课表/日历/弹窗共用）
getCoursesOfDate(dateStr) -> Course[]
  // 1. 命中 holidays  → 返回 []
  // 2. 命中 adjustments → weekday = targetWeekday，过滤周号 = 当天教学周号
  // 3. 否则 → weekday = date 的绝对星期，过滤周号 = 当天教学周号
  // 再对 weekday 匹配 + isCourseOnWeek(course, weekNum) 过滤，按 startSection 排序
```

**测试用例表（纯函数可直接单测，建项目时先写这些）：**

| 用例 | 输入 | 期望 |
|------|------|------|
| 周号-正常 | termStart=周一，第 7 天 | weekNum=2 |
| 周号-假期不跳号 | 第 5 周内放假 3 天，仍查该周日期 | weekNum 仍=5 |
| 单双周-首周单 | firstWeekType=odd，第 2 周 | 双周 |
| 单双周-首周双 | firstWeekType=even，第 1 周 | 双周（isOddWeek=false） |
| 手动周次 | manualWeek=6（firstWeekType=odd） | 双周 |
| 调休-单双周 | 双周的周六补周三课，该课仅单周 | 不显示 |
| 周段解析 | "2-8,10-14" | 命中 7、14；不命中 9 |
| 假期优先 | 同日既是假期又是调休 | 无课程 |
| 月历-国庆 | 假期内的周三 | 无课程、置灰 |

---

## 六、后端爬虫框架（FastAPI + Playwright）

### API 契约（唯一接口）

```
POST /api/import
req:  { "school": "zhengfang", "studentId": "...", "password": "...", "captcha": "..." }
resp ok:  { "code": 0, "data": { "courses": [...], "warnings": ["..."] } }
resp err: { "code": 1, "message": "验证码错误 / 登录失败 / 课表为空" }
```

- `data.courses` 元素结构**与前端 `courses` 完全一致**（含 `weeks`、`weekday`、`startSection` 等），后端负责转换周规则为统一格式；`id` 由前端生成，`sourceKey` 由后端给出。
- 请求只在内存中保留凭据，**响应返回后即丢弃，任何日志/异常信息不落盘**；服务器必须启用 HTTPS。

### 适配器结构（模板，解析逻辑留空由你补充）

```
backend/
  app.py                 # FastAPI 入口，/api/import 路由
  import_service.py      # 编排：登录 → 打开课表页 → 解析 → 转统一格式
  adapters/
    base.py              # SchoolAdapter 抽象类：login() / fetchScheduleHtml() / parse()
    zhengfang.py         # 正方适配器模板（占位，后续填选择器/正则）
  playwright_client.py   # Playwright 封装：浏览器单例、超时、重试、会话清理
```

- 【加】Linux 部署注意：`playwright install chromium` 及其系统依赖（`playwright install-deps`），否则启动报缺库。
- 【加】正方教务常见坑：验证码需要前端截图展示→用户输入→随请求提交；登录后 session 复用；页面若改版只影响对应适配器，接口契约不变。
- 【加】为防学校改版后接口不可用，前端模式 B（粘贴解析）永远是可用兜底。

---

## 七、页面/组件/目录结构（前端）

```
pages/
  schedule/  schedule.vue    # 周课表（swiper + 网格 + 参考线）
  calendar/  calendar.vue    # 月历（自封装月历 + 日期弹窗）
  settings/  settings.vue    # 学期配置、节次、假期、调休、导入、备份
  import/    importPreview.vue  # 导入预览（新增/更新/冲突分组）
components/
  weekGrid/     # 周网格（含同节次并排算法）
  courseCard/   # 课程块
  daySheet/     # 当日课程底部弹窗
  monthCal/     # 月历组件
utils/
  week.js  filter.js  weeksPattern.js  time.js   # 纯函数
  parser/  htmlParser.js  textParser.js          # 模式B解析器（接口+正方模板实现）
store/
  index.js        # 存储管理器（读写/备份/恢复/版本迁移），唯一触碰 storage 的地方
  useData.js      # 组合式 API 封装，页面订阅数据
api/
  importApi.js    # 模式A 接口调用（baseURL 来自 config 文件）
```

---

## 八、实施路线图

| 阶段 | 内容 | 验收 |
|------|------|------|
| P0 | 数据模型 + 存储管理器 + 三个纯函数模块 + 测试用例 | 测试全过，存储读写备份恢复可用 |
| P1 | 周课表页（内置示例数据跑通） | 单双周/周段/滑动/参考线正常 |
| P2 | 课程 CRUD、调休假期管理、日历页 | 两页渲染同一过滤链结果一致 |
| P3 | 导入模式 B（粘贴解析）→ 模式 A（后端接口） | 导入预览/覆盖/增量/冲突提示可用 |
| P4 | 打磨：冲突并排、备份恢复、深色模式、体验版发布 | 完整可用 |

建议顺序：**P0 纯函数先行**（核心逻辑全部可测），页面只是它们的展示层。

---

## 九、风险清单

1. **教务网页改版** → 爬虫失效：模式 B 兜底 + 适配器隔离，影响面只在一个适配器。
2. **正方验证码识别** → 人工输入框方案，不追求 OCR。
3. **微信体验版成员上限**（默认约 15 人，以平台规则为准）→ 几十人规模要么上架（个人主体类目受限）要么考虑顺带编译 App 形态分发；uni-app 低成本支持，作为 P4 之后的备选。
4. **存储上限** → 数据量 KB 级无压力；写入失败有兜底提示 + 导出备份。
5. **密码经服务器中转** → 个人服务器 + HTTPS + 内存即用即弃 + 不落盘；介意可用模式 B 完全本地。
